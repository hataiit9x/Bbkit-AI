#!/usr/bin/env bash
set -euo pipefail
source "$BB_ROOT/lib/common.sh"

[[ -f "$BB_AI_ROOT/tools/validate.py" ]] || {
  err "validate tool not found at $BB_AI_ROOT/tools/validate.py"
  exit 1
}

cd "$BB_AI_ROOT"
exec python3 "$BB_AI_ROOT/tools/validate.py" "$@"
